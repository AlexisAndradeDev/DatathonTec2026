"""Construcción de la feature matrix (~3,200 columnas) para clustering."""

def run_build_matrix(input_dir: str = "data/processed/", output_path: str = "data/processed/feature_matrix.parquet") -> None:
    """Joinea todas las features enriquecidas por user_id y construye la matriz final."""
    pass


if __name__ == "__main__":
    run_build_matrix()
