"""Carga y joins iniciales de datasets."""

def run_ingest(raw_dir: str = "data/raw/", processed_dir: str = "data/processed/") -> None:
    """Carga datasets, realiza joins iniciales y guarda versiones limpias."""
    pass


if __name__ == "__main__":
    run_ingest()
