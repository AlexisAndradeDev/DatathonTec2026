"""Validación de schemas de los 4 datasets."""

def run_validate(data_dir: str = "data/raw/") -> None:
    """Valida schemas, nulls, FK integrity y duplicados."""
    pass


if __name__ == "__main__":
    run_validate()
